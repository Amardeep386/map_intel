# Portal settings (Vite). Copy to .env.local to change them locally; set them in Vercel for production.
# true (default): all screens use built-in sample data, no backend needed.
# false: sign-in, clients and Product Summary come from the MAP Intel API (server/).
VITE_USE_MOCK=true
VITE_API_URL=http://localhost:4000
